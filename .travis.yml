language: python
python:
  -  "3.8"
install:
  - pip install -r requirements.txt
  - pip install flask
  - pip install flask_script
  - pip install flask_sqlalchemy
env:
  -  APP_SETTINGS=config.DevelopmentConfig DATABASE_URL=postgresql://USERNAME:PASSWORD@localhost/DBNAME
script:
  - nosetests
   # - pytest
   # - python AuthMicroservice/manage.py runserver
